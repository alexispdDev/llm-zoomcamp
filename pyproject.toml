[project]
name = "llm-zoomcamp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alexispd", email = "alexispd@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "gitsource==0.0.5",
    "minsearch==0.1.0",
    "openai==2.41.1",
    "python-dotenv==1.2.2",
    "requests==2.34.2",
    "sqlitesearch==0.1.1",
    "toyaikit==0.0.11",
]

[project.scripts]
llm-zoomcamp = "llm_zoomcamp:main"

[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel==7.3.0",
]
